upload_max_filesize = 100M
post_max_size = 108M

; Hand every outgoing mail to msmtp, which the image points at the Mailhog
; container. Read your test e-mail at http://localhost:5025
sendmail_path = "/usr/bin/msmtp -t"
